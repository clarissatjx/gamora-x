# Upper bounds are deliberate: Streamlit Community Cloud resolves these at deploy time,
# so an unbounded spec would rebuild the app from whatever ships months from now. The
# theme matches Streamlit's internal data-testid/:has() structure, which is exactly what
# a major release is free to rename.
pandas>=2.1,<3
numpy>=1.26,<3
scikit-learn>=1.3,<2
scipy>=1.13,<2
joblib>=1.3,<2
openpyxl>=3.1,<4
streamlit>=1.37,<2
altair>=5,<7
